route:
  receiver: 'telegram'
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
- name: 'telegram'
  telegram_configs:
  - bot_token: "{{ telegram_token }}"
    chat_id: {{ telegram_chat_id }}
    send_resolved: true
